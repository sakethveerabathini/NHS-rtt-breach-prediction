## Data Preparation – Master Dataset Creation

The cleaned NHS RTT datasets were organised into yearly folders.

Each folder contained a pre-processed dataset (RTT_FULL_DATA.csv).

Steps performed:
- Iterated through all yearly folders
- Combined all datasets into one master dataset
- Added year reference column
- Standardised column names

Output:
- rtt_master_data.csv